apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uaa-auth-srv.fullname" . }}-test-connection"
  labels:
    {{- include "uaa-auth-srv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uaa-auth-srv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
