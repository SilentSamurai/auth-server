export enum Action {
    Manage = "manage",
    Create = "create",
    Read = "read",
    Update = "update",
    Delete = "delete",
    ReadCredentials = "read-credential",
}

export enum Effect {
    ALLOW = "ALLOW",
    DENY = "DENY",
}
